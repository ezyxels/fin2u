import { Widget } from "@typeform/embed-react";
import type { NextPage } from "next";

const CarrerTest: NextPage = () => {
  return <Widget id="RgnYsccT" style={{ width: "100vw", height: "100vh" }} />;
};

export default CarrerTest;
